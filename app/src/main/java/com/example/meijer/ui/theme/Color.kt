package com.example.meijer.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Meijer brand colors based on the app design.
 * Primary blue for headers and buttons, red for special labels, etc.
 */

// Primary Meijer Blue - Used for headers, buttons, and active states
val MeijerBlue = Color(0xFF0066CC)

// Meijer Red - Used for special labels and highlights
val MeijerRed = Color(0xFFE31837)

// Text Colors
val MeijerTextPrimary = Color(0xFF000000) // Black for main text
val MeijerTextSecondary = Color(0xFF666666) // Gray for secondary text
val MeijerTextPlaceholder = Color(0xFF999999) // Light gray for placeholders
val MeijerTextOnBlue = Color(0xFFFFFFFF) // White text on blue background

// Background Colors
val MeijerBackground = Color(0xFFFFFFFF) // White background
val MeijerSurface = Color(0xFFFFFFFF) // White surface
val MeijerSearchBar = Color(0xFFFFFFFF) // White search bar

// Border and Divider Colors
val MeijerBorder = Color(0xFFE0E0E0) // Light gray border
val MeijerDivider = Color(0xFFE0E0E0) // Light gray divider

// Tab Colors
val MeijerTabActive = MeijerBlue // Blue for active tab
val MeijerTabInactive = Color(0xFF999999) // Gray for inactive tab

// Button Colors
val MeijerButtonPrimary = MeijerBlue // Blue buttons
val MeijerButtonText = Color(0xFFFFFFFF) // White text on buttons

// Legacy colors (kept for compatibility, but not used)
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)
val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
